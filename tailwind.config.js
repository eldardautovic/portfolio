/** @type {import('tailwindcss').Config} */
module.exports = {
    theme: {
        extend: {
            fontSize: {
                'xs': '0.75rem', // This adds the text-xs utility class
            },
        },
    },
    // ... rest of your existing configuration
    plugins: [],
}